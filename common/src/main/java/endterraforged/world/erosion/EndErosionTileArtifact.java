package endterraforged.world.erosion;

/** Primitive artifact contract shared by the P4.7 candidate cache. */
interface EndErosionTileArtifact {

    EndErosionTileKey key();

    long primitiveBytes();
}
